/**
 * Defines the keys used for storing and retrieving puzzle-related data in localStorage.
 */
const STORAGE_KEYS = {
    /**
     * @constant {string} PUZZLE_STATE - The key used to store or retrieve the current puzzle state.
     */
    PUZZLE_STATE: "puzzleState",

    /**
     * @constant {string} PUZZLE_CONFIG - The key used to store or retrieve the configuration settings of the puzzle.
     */
    PUZZLE_CONFIG: "puzzleConfig"
}